# this file tests whether we are talking to  version 11 or higher of jdb.

{ EXPECT( 3, -re, "Initializing jdb");	}	# jdb output upon launch 
{ SEND( 	  "version\n" ); }		# remember to send the newline!
{ EXPECT( 3, -re, "jdb version 11");	}	# if we get this pattern, that's a PASS
