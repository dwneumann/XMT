# this file tests whether Hello.jar outputs the expected "hello foo".

{ EXPECT( 3, -re, "Initializing jdb");	} # jdb output upon launch 
{ SEND( 	  "run\n" ); 		} # remember to send the newline!
{ EXPECT( 3, -re, "hello foo");		} # if we get this pattern, that's a PASS
