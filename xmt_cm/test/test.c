#ifdef EMBED_REVISION_STRINGS
static const char test_c_id[] = "@(#) xmt_cm::test.c	$Version:$";
#endif

int main(int argc, char *argv)
{
    short me;
    short i;

    for (i=0; i< 10; ++i)
    {
        printf("hello from me to %d\n, i);
    }
    printf("fini\n");
}

